import React from "react";

const MessageInput = () => {
  return (
    <div>
      <input type="text" placeholder="Write a new message" />

      <button>Send</button>
    </div>
  );
};

export default MessageInput;
